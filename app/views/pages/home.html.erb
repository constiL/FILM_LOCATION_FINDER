
  <form novalidate="novalidate" class="simple_form search" action="/" accept-charset="UTF-8" method="get">
  <div class="search-form-control form-group">
    <input class="form-control string required" type="text" name="search[query]" id="search_query" />
    <button name="button" type="submit" class="btn btn-flat">
      <i class="fas fa-search"></i> Search
    </button>
  </div>
</form>

<%# <div class="main-content">
  <div class="listing-card">
    <img src="<%= asset_path('location-1.jpg') %><%#" alt="image of location">
  </div>
  <div class="location-details">
    <div class="location-title">Cool Spot</div>
    <div class="location-description">bla bla bla</div>
  </div>
</div> %>

    <div class="main-content">
        <div class="row g-4 align-items-stretch">

          <% @locations.first(4).each do |location| %>
            <%= link_to location_path(location) do %>
              <div class='dashcard' style="background-image: linear-gradient(rgba(0,0,0,0.5),rgba(0,0,0,0.5)), url(<%= location.image_url %>)">
                <div class='card-content'>
                  <h4><%= location.name %></h4>
                </div>
              </div>
            <% end %>
          <% end %>

<%# <div id="map" style="width: 100%; height: 300px;"></div>%>
<div id="map" style="width: 100%; height: 600px;"
  data-controller="map"
  data-map-markers-value="<%= @markers.to_json %>"
  data-map-api-key-value="<%= ENV['MAPBOX_API_KEY'] %>">
</div>
